[workspace]
members = [".", "migration"]

[package]
name = "support_cafe"
version = "0.1.7"
edition = "2024"

[[bin]]
name = "worktable_rebuild"
path = "src/bin/worktable_rebuild.rs"
required-features = ["s3-sync"]

[dependencies]
async-trait = "^0.1"
chrono = { version = "^0.4", default-features = false, features = ["oldtime", "std", "now"] }
clap = { version = "^4.5", features = ["derive"] }
config = { version = "^0.15", default-features = false, features = ["toml"] }
crossfire = "^3.1.10"
derive_more = { version = "^2.1.1", features = ["full"] }
endpoint-libs = { version = "^3.2", features = ["ws", "ws-http1"] }
eyre = "^0.6"
futures = "^0.3"
num-derive = "^0.5"
num-traits = "^0.2"
parking_lot = "^0.12"
psc-nanoid = { version = "^3.2", features = ["serde", "packed", "rkyv"] }
rkyv = "^0.8.12"
secrecy = "^0.10.3"
serde = { version = "^1.0", features = ["derive"] }
serde_json = "^1.0"
smart-default = "^0.7"
strum = "^0.28"
strum_macros = "^0.28"
nagoya = { version = "^0.1", features = ["reactor"] }
nago-rustls = "^0.1"
nago-http = "^0.1.1"
nago-telegram = "^0.1"
tracing = "^0.1"
url = "^2.5.7"
worktable = { version = "^1.10.0-beta1" }
honey_id-types = "^2.1"
cert-provider = { package = "ps-cert-provider", version = "^0.1", features = ["dns01"], optional = true }

[features]
s3-sync = ["worktable/s3-support"]
# Off by default: Fly terminates TLS, and endpoint-libs 3 refuses a TLS config.
# Kept for a deployment that has to obtain its own certificates.
acme = ["dep:cert-provider"]
cert-s3-sync = ["acme", "cert-provider/s3-sync"]
